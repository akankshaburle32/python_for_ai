

def average(nums: list[int]) -> float:
    """
        Gives the average value of given list the no.

        argu :
                nums (list[int]) list of no.

        Returns :
                float : average value of given list of no. 
    """

    return sum(nums) / len(nums)


def median(nums: list[int]) -> float:
    """
        Gives the median value of given list the no.

        argu :
                nums (list[int]) list of no.

        Returns :
                float : median value of given list of no. 
    """


def mode(nums: list[int]) -> float:
    """
        Gives the mode value of given list the no.

        argu :
                nums (list[int]) list of no.

        Returns :
                float : mode value of given list of no. 
    """